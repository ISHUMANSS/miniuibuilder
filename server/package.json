{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^5.1.0",
    "groq-sdk": "^0.5.0",
    "mongodb": "^6.20.0"
  },
  "scripts": {
    "server": "node server.js",
    "build" : "npm install --prefix ./frontend && npm install react-router-dom --prefix ./frontend && npm run build --prefix ./frontend  && npm install && npm i groq-sdk"
}
}
